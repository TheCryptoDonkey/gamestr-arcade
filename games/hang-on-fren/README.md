# Hang On, Fren

Web tile for the Riviera Vespa road-tribute racer. Runs in kiosk web-frame
mode from the public GitHub Pages deployment; scores publish as kind-30762
events with `game` = `hangonfren`, so the leaderboard needs no extra config.

The game supports guest mode (local keypair) and NIP-07 nostr sign-in.
